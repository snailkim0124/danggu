## 참고 문서

- PRD: docs/PRD.md
- 구현 계획: docs/PLAN.md
- Deep Interview 원본 기록: docs/deep-interview-danggu-4gu-path-guide.md (근거 확인용, 실행 기준 아님)

**작업 전 반드시 위 문서를 읽고 시작할 것.** PRD/PLAN과 충돌하는 임의 해석·설계 변경 금지. 문서에 없는 결정이 필요하면 코드를 먼저 짜지 말고 질문할 것.

## 도메인 모델 일관성

- `PLAN.md`의 Ontology(Shot/Table/Ball/Photo)에 정의된 필드명을 코드의 타입/인터페이스 이름과 **1:1로 정확히 일치**시킬 것. 예: `missMargin`, `recognitionStatus`, `difficultyScore`, `ruleValid`, `forceLevel` — 문서와 코드가 따로 이름 짓지 말 것
- 문서에 없는 새 필드를 추가해야 하면, 먼저 PLAN.md를 업데이트하고 나서 코드에 반영할 것 (코드가 문서보다 앞서가지 않도록)
- 물리적으로 다른 단위(각도 vs 라디안, 비율 좌표 vs mm 실측, 이산 forceLevel vs 연속 수치)를 섞어 쓰지 말 것. 변수명에 단위를 명시 (`angleDeg`, `ratioX` 등)

## TypeScript 규칙

- `any` 금지. 불가피하면 `unknown` + 타입 가드로 좁힐 것. `as any` 절대 금지
- `tsconfig.json`의 `strict: true` 유지, 임의 완화 금지
- 타입 전용 import는 `import type { X } from '...'`로 명시
- API 입출력은 Zod 스키마로 검증, 타입은 `z.infer`로 스키마에서 파생 (스키마가 단일 진실 소스)
- catch한 에러는 `unknown`으로 취급 (`error instanceof Error ? error.message : String(error)`)
- 매직 넘버 금지 — 테이블 규격(2540mm 등), 임계값(70%, 8mm RMS 등)은 상수로 한 곳에 모을 것

## 인식 실패 처리 원칙 (Vision Recognition)

- 인식이 애매하거나 실패할 수 있는 케이스(공이 서로 붙어있음, 조명 불량, 그림자로 인한 오분류 등)를 발견하면, **조용히 틀린 결과를 반환하지 말 것**. 신뢰도가 낮다고 판단되면 반드시 `recognitionStatus`를 `uncertain`으로 전이시켜 사용자 보정 화면으로 분기시킬 것
- "일단 그럴듯하게 추정해서 반환"하는 방식 금지 — 이 도메인은 오인식이 사용자에게 잘못된 샷을 실전에서 치게 만드는 직접적 피해로 이어짐
- 새로운 인식 실패 케이스를 발견하면 관련 유닛테스트(픽스처 이미지 또는 좌표 케이스)를 함께 추가할 것

## 규칙 필터 (ruleValid) 관련

- 상대공 접촉, 한쪽 빨간공만 접촉 등 무효 샷은 반드시 `ruleValid: false`로 걸러낼 것
- **키스(의도치 않은 다른 공과의 충돌)**를 경로 계산에서 반드시 고려할 것. "경로 비폐색" 조건에 키스 회피가 포함되는지 애매하면 코드로 임의 결정하지 말고 먼저 확인할 것

## 테스트 및 커밋 전 체크

- 새 함수/모듈 작성 시 대응하는 테스트 파일을 같은 디렉토리에 항상 생성
- 테스트 없이 "작동합니다"라고 보고하지 말 것 — 실제로 `npm test` 실행 결과를 확인 후 보고
- 커밋 전 반드시 `npm run build && npm run lint && npm test` 통과 확인. 실패 시 커밋하지 말고 원인부터 해결할 것

## 범위 관리 (Out of Scope 준수)

- PRD의 Out of Scope(회원가입/로그인/세션 관리, 다중 사용자, 알림/캘린더 연동, 모바일 네이티브 앱, 개인화 난이도 설문)에 해당하는 코드를 암묵적으로 추가하지 말 것
- 단, 배포 접근 제어(Vercel Password Protection 또는 미들웨어 토큰 등)는 인증 기능이 아니라 데이터 보호 조치로 별도 허용됨 (PLAN.md §5 참고)

## Phase 및 세션 관리

- Phase 단위(Phase 0~4)로 작업을 끊어서 진행할 것. 한 Phase가 끝나면 다음 Phase로 넘어가기 전 진행 상황을 커밋하고 요약할 것
- Phase 5(플레이 게이트)는 사람이 실제로 당구를 쳐서 검증하는 단계이므로 자동 진행/자동 판정 대상에서 제외. 관련 코드는 결과 기록 도구까지만 준비하고, 판정 자체를 시뮬레이션하거나 대신 수행하지 말 것
- 컨텍스트 사용량이 많이 쌓였다고 판단되면(긴 세션, 다수 파일 탐색 등) 작업을 무리하게 이어가지 말고 현재까지 진행 상황을 명확히 요약해 남길 것 — 이후 세션에서 압축/재시작되어도 복구 가능하도록

## 보고 방식

- "완료했습니다"라고만 보고하지 말고, 어떤 파일을 바꿨는지, 어떤 테스트를 돌렸고 결과가 어땠는지 구체적으로 보고할 것
- 문서(PRD/PLAN)와 다르게 구현할 수밖에 없었던 부분이 있으면 반드시 명시적으로 알릴 것 (조용히 다르게 구현하지 말 것)
