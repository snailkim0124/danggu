# Plan: 4구(사구) 당구 샷 경로 자동 안내 서비스

- Status: **pending approval**
- Mode: Direct (기존 Deep Interview 명세 + PRD 기반, Analyst 리스크 검토 반영)
- Source artifacts: `.omc/specs/deep-interview-danggu-4gu-path-guide.md` (18라운드, 최종 모호도 14%), `PRD.md`
- Generated: 2026-08-28

## Requirements Summary

사용자가 모바일 웹에서 4구 당구대 사진 1장을 업로드하면, 시스템이 테이블 경계와 공 4개(흰공1/노랑공1/빨간공2)를 완전 자동으로 인식하고, 직접샷부터 뱅킹샷·고급 기술샷(스핀 포함)까지 모든 가능한 캐롬 경로를 계산해, 기술 난이도 기준 상위 3개 후보를 실전에서 바로 따라칠 수 있는 수준의 수치와 함께 정적 2D 다이어그램으로 보여준다. 점수 기록/멀티플레이어/개인화 설문은 이번 버전 범위에서 제외한다.

## Key Decisions (이번 세션에서 확정)

| # | 결정 | 근거 |
|---|------|------|
| D1 | **고급기술샷(끌어치기/밀어치기/회전) v1 포함 유지** — 원래 인터뷰 결정 그대로 | 사용자가 Analyst 리스크 브리핑 이후에도 명시적으로 유지 선택. Shot 데이터 모델과 물리 엔진 범위를 이에 맞춰 확장해야 함 |
| D2 | **MVP "적중" = 득점(큐볼이 빨간공 2개 모두 접촉, 상대공 파울 없음)**, 10건 테스트 배치 구성을 사전 고정 | 기존 정의("제시된 경로대로 치면 맞다")는 시스템 오류와 사용자 실행 오류를 구분 못함. 득점 기준 + 고정 배치가 재현 가능한 판정 기준 |

이 두 결정은 `PRD.md`/`deep-interview-danggu-4gu-path-guide.md`의 해당 항목을 대체한다 (Non-Goals·Ontology 등 나머지 항목은 그대로 유효).

## 채택된 엔지니어링 기본값 (Analyst 권고 반영, 낮은 리스크 — 필요시 언제든 재검토 가능)

- **힘(force) 표현**: 연속 수치(m/s 등) 대신 관찰 가능한 **이산 3~5단계**로 표현 (예: "쿠션 1개 돌 세기" ~ "쿠션 3개 돌 세기"). 절대 물리량은 재현 불가능하지만 이산 단계는 사람이 재현 가능.
- **테이블 규격 설정값 추가**: 대대(2540×1270mm) / 중대(2438×1219mm) 프리셋을 큐볼 색상과 함께 설정에 저장. 사진만으로는 절대 스케일 복원이 불가능하므로 힘 계산에 필요.
- **인식 확인 화면 분리**: "원본 사진 위에 인식된 공/경계를 오버레이해서 사용자가 확인·보정하는 화면"과 "결과를 보여주는 정적 다이어그램 화면"을 별개 단계로 둔다. 확인 화면이 없으면 큐볼 색상 오인식(전체 추천이 조용히 100% 틀리는 치명적 실패)을 사용자가 알아챌 방법이 없음.
- **테스트를 2단계 게이트로 분리**: (a) 지오메트릭 게이트 — 인식 정확도만 측정(사람 개입 없음), (b) 플레이 게이트 — D2 기준 실전 적중률. 지오메트릭 게이트를 먼저 통과시켜야 플레이 게이트 결과가 의미를 가짐.

## Technology Stack (확정)

| 영역 | 선택 | 근거 |
|------|------|------|
| 언어 | TypeScript/JavaScript (Node.js) 풀스택 | 사용자 기존 숙련도 |
| 프론트엔드 | Next.js (React) | 모바일 브라우저 카메라/업로드, Vercel과 궁합 최적 |
| 백엔드 | Next.js API Routes / Vercel Serverless Functions | 배포 단순화(사용자 선택: Vercel) |
| Vision(CV) | **OpenCV.js (WASM 빌드)** | Vercel 서버리스 환경은 네이티브 바인딩(opencv4nodejs) 컴파일을 지원하지 않으므로 채택. 순수 JS/WASM이라 서버리스·브라우저 모두에서 동작 |
| 데이터 저장 | **MongoDB (Atlas, 클라우드 호스팅)** | 사용자 지정. 큐볼 색상·테이블 규격 설정을 서버 측에 저장 (v1은 로컬 스토리지 대신 이 방식 채택) |
| 물리/경로 계산 | 순수 TypeScript (외부 네이티브 의존성 없음) | 벡터 연산·쿠션 반사각 계산은 네이티브 라이브러리 불필요, 서버리스 제약과 무관 |
| 호스팅 | Vercel (프론트+백엔드), MongoDB Atlas (DB) | 사용자 선택: "일단 Vercel로" |

**⚠️ 트레이드오프 기록:** OpenCV.js(WASM)는 opencv4nodejs 대비 API가 제한적이고 실행 속도가 느립니다. Vercel 서버리스 함수는 실행 시간·메모리 제한이 있어 고해상도 사진의 라인 검출/호모그래피 계산이 무거우면 타임아웃 위험이 있습니다 → Risks 표에 반영, 업로드 시 이미지 다운스케일(예: 최대 변 1600px)을 Phase 1 필수 스텝으로 추가.

## Architecture / Data Model

### Shot (확장)
| 필드 | 설명 |
|------|------|
| technique | direct / bank(N쿠션) / advanced(스핀) |
| aimTarget | 목적구 위 조준점(두께 비율) — "실전에서 따라칠 수 있는" 1차 표현. 각도(도)는 보조 표시 |
| angleDeg | 각도 수치(보조 표시) |
| forceLevel | 이산 단계(1~5) |
| tipOffset | {vertical, horizontal} — 고급기술(스핀)용, v1 direct/bank는 항상 center |
| thickness | 두께 비율 (1/2, 1/4 등) |
| sequence | 맞히는 공 순서 |
| difficultyScore | **각도-허용폭(angular tolerance)** 기반 연속 점수. 기술종류(technique)를 1차 정렬키로, 허용폭을 2차(tie-break)로 사용 |
| ruleValid | 규칙 필터(양쪽 빨간공 접촉 + 상대공 무접촉 + 경로 비폐색) 통과 여부 — false면 후보에서 제외 |
| confidence | 이 샷 추천의 신뢰도(인식 오차 대비 허용폭이 좁으면 낮음 → low-confidence 표시 또는 억제) |

### Table
| 필드 | 설명 |
|------|------|
| boundary | **쿠션 노즈 라인**(공이 실제로 구르는 안쪽 경계) 기준 4개 선분 — 코너 점이 아니라 4변 직선의 교점으로 계산(모서리가 프레임 밖으로 잘려도 복원 가능) |
| size | 대대 / 중대 프리셋 (설정값) |

### Ball
| 필드 | 설명 |
|------|------|
| position | 클로스 평면(z=0)이 아닌 **z=공 반지름 평면**으로 역투영 보정된 실측 좌표 |
| color | 흰/노랑/빨강×2 — 흰/노랑은 **두 후보 간 상대적 색상 비교**로 분류(절대 임계값 금지, 조명에 취약) |
| role | cueBall(설정값 기반) / opponentBall / targetBall×2 |

### CalibrationConfidence
- 인식 신뢰도 수치화 → 임계값 미만이면 사진 오버레이 확인/보정 화면으로 분기

## Implementation Steps

### Phase 0 — 스캐폴딩
1. Next.js 프로젝트 초기화 (모바일 브라우저 카메라/파일 업로드 지원), Vercel 배포 파이프라인 연결
2. MongoDB Atlas 클러스터 생성 + Next.js API Routes에서 서버리스 친화적 연결 캐싱(connection reuse) 설정
3. OpenCV.js(WASM) 초기 로딩/번들 크기 검증 — 서버리스 함수 콜드스타트에 미치는 영향 확인
4. 설정 스키마(큐볼 색상 + 테이블 규격 프리셋) MongoDB 컬렉션 설계

### Phase 1 — Vision Recognition (시차 보정을 처음부터 반영, 나중에 추가하는 리팩터링 아님)
1. **업로드 이미지 다운스케일**(예: 최대 변 1600px) — Vercel 서버리스 실행시간/메모리 제한 대응, CV 연산 부하 축소
2. 쿠션 4변 라인 검출 → 교점으로 테이블 경계 산출 (코너 아웃오브프레임 대응) — OpenCV.js
3. 호모그래피 계산 및 카메라 포즈 복원 — OpenCV.js
4. 공 4개 검출 → **z=반지름 평면**으로 역투영해 실측 좌표 산출
5. 흰/노랑 상대색상 분류, 빨간공 2개 식별
6. 인식 신뢰도 산출 → 임계값 미만 시 사진 오버레이 확인/보정 화면(터치 기반) 표시
7. **검증**: 지오메트릭 게이트용 mm 오차 측정 하네스 구축 + 서버리스 함수 실행시간 측정(타임아웃 마진 확인) (Phase 5에서 사용)

### Phase 2 — Path Calculation
1. 아핑-각도 스윕 기반 직접샷 후보 열거 + 규칙 필터(양쪽 빨간공 접촉/상대공 무접촉/비폐색) 적용
2. 1~2쿠션 뱅킹샷 후보 — 쿠션 반사각 계산(반발계수 근사치 포함, 이산 forceLevel과 연동)
3. 고급기술샷(스핀) 후보 — tipOffset/thickness 기반, 스로우(throw)·스쿼트(squirt) 근사 모델. **범위가 크므로 direct+bank가 지오메트릭 게이트를 통과한 뒤 별도 스프린트로 진행**
4. 각 후보의 angular-tolerance 기반 difficultyScore 산출 → 기술종류 1차 정렬, tolerance 2차 정렬
5. 유효 후보 0개 시 근접 샷(최소 오차 후보) fallback 로직

### Phase 3 — Result Visualization
1. 정적 2D 다이어그램 렌더링 (테이블/공/경로 재구성)
2. 상위 3개 후보 탭/스와이프 전환 UI
3. low-confidence 후보 시각적 구분 표시

### Phase 4 — 설정 & 온보딩
1. 큐볼 색상 프리셋 UI → MongoDB에 저장/조회하는 API Route
2. 테이블 규격(대대/중대) 프리셋 UI → 동일 컬렉션에 저장

### Phase 5 — 검증 (D2 반영)

**담당 구분: Claude(코드/자동화) vs 사용자(물리적 실행)**

1. **지오메트릭 게이트 — Claude 구현 범위 (자동화 가능, 실제 당구 안 쳐도 됨)**
   - 측정 하네스 코드 작성: 사진 업로드 → 인식 결과(공 중심/경계 좌표) 출력 → 정답 좌표와 mm 오차 비교 → 리포트 생성
   - 오차 임계값(예: ≤8mm RMS) 자동 판정 로직 포함
   - **사용자 협조 필요한 입력물**: 다양한 배치의 실제 사진 N≥30장 + 각 사진의 정답 좌표(탑뷰 사진 촬영 또는 직접 자로 측정). 이 입력물 수집은 사용자 몫이지만, 사진은 당구를 "치지" 않고 배치만 세팅해 찍으면 되므로 플레이 게이트보다 훨씬 가볍습니다
   - 이 게이트는 Claude가 리포트까지 자동 생성해서 통과/실패를 스스로 판단 가능

2. **플레이 게이트 — 사용자 실행 필수 (Claude가 대신할 수 없는 물리적 검증)**
   - Claude가 준비: 배치 10건 사양(예: 직접샷 6 / 1쿠션 뱅킹 3 / 무해 1)을 문서화하고, 사용자가 그대로 따라 세팅할 수 있는 체크리스트 + 결과 기록용 템플릿(표/스프레드시트) 제공
   - 사용자가 실행: 실제 당구대에서 배치 세팅 → 사진 촬영 → 앱이 추천한 샷대로 직접 치기(배치당 시도 횟수 사전 합의, 예: 3회) → "적중"(빨간공 2개 접촉, 상대공 파울 없음) 여부와 본인이 직접 고른 샷의 성공률(베이스라인)을 템플릿에 기록
   - 사용자가 기록을 넘겨주면 Claude가 결과를 집계해 70% 기준 통과 여부 및 실패 패턴(어떤 기술 종류에서 주로 실패하는지 등) 분석

## Risks and Mitigations

| 리스크 | 실패 시나리오 | 완화책 |
|--------|--------------|--------|
| 공 반지름 시차 미보정 | 2m 거리 공이 60~90mm(공 1~1.5개) 밀려 보임 → 각도 오차 ~4.5° | Phase 1에서 z=반지름 평면 역투영을 최초 설계에 반영(사후 패치 아님) |
| 힘(force) 물리 예측 불가 변수 의존 | 쿠션 반발계수·클로스 마찰이 사진에서 관측 불가 → 연속 수치가 거짓 정밀도 | 이산 forceLevel로 표현, 반발계수는 근사 상수 + 실측 보정 |
| 고급기술샷 데이터 모델 미표현 | angle+force만으론 스핀 샷 표현 불가 | Shot에 tipOffset/thickness 필드 추가(D1에 따라 v1부터 구현) |
| 인접 테이블/색상 오분류로 큐볼 스왑 | 전체 추천이 100% 틀리지만 겉보기엔 정상 | 상대색상 분류 + 사진 오버레이 확인 화면 필수 |
| 후보 생성이 파울 샷을 걸러내지 못함 | 상대공 접촉/한쪽 빨간공만 접촉하는 무효 샷 추천 | ruleValid 하드 필터(양쪽 빨간공 접촉 + 상대공 무접촉 + 비폐색) |
| N=10 플레이 테스트가 통계적으로 저신뢰 | 완벽한 시스템도 배치 구성에 따라 70% 미달 가능 | 지오메트릭 게이트로 시스템 정확도 별도 검증, 배치 구성 사전 고정, 인간 베이스라인 병행 기록 |
| Vercel 서버리스 함수 실행시간/메모리 제한 | 고해상도 사진의 라인 검출·호모그래피 계산이 타임아웃/OOM으로 실패 | 업로드 시 이미지 다운스케일(최대 변 1600px), Phase 1에서 실행시간 실측 후 필요 시 처리 단계 분할(멀티 스텝 함수 호출) |
| MongoDB 서버리스 연결 오버헤드 | 함수 콜드스타트마다 새 DB 연결 생성 → 연결 폭주/지연 | Mongoose 연결 캐싱 패턴(글로벌 변수 재사용) 적용, Atlas 연결 풀 크기 조정 |

## Verification Steps

**Claude가 자동으로 검증 (구현 완료 기준):**
- [ ] 규칙 필터 단위 테스트: 상대공 접촉/한쪽만 접촉하는 경로가 후보에서 제외되는지 확인
- [ ] 흰/노랑 분류가 다양한 조명 샘플에서 상대색상 기준으로 안정적인지 확인
- [ ] 인식 신뢰도 임계값 미만 시 확인/보정 화면이 실제로 트리거되는지 확인
- [ ] 유효 후보 0개 배치에서 근접 샷 fallback이 표시되는지 확인
- [ ] 지오메트릭 게이트: 사용자가 제공한 사진+정답 좌표 세트로 mm 오차 임계값 이하 달성 (측정 하네스가 자동 리포트 생성)
- [ ] Vercel 함수 실행시간이 타임아웃 한도 내에 있는지 실측

**사용자가 직접 수행 필요 (Claude가 대신 못 함):**
- [ ] 플레이 게이트: 고정 10건 배치, 배치당 시도 횟수 준수, 득점 기준 채점 결과 70%+ (테스터 베이스라인과 함께 기록 → 결과를 Claude에 전달하면 집계/분석)

## Open Follow-ups (v1 이후)

- 개인화 난이도 설문(User Skill Profile) — v2
- 고급기술샷 스핀 시뮬레이션 정밀도는 Phase 5 실측 이후 튜닝
- 클로스/쿠션 물성치 보정 계수는 실제 당구장 환경별 편차 존재 — 추후 현장 캘리브레이션 고려
